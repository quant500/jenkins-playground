pipeline {
    
    agent { any label 'master' }
    
    stages {
        stage('build') {
            steps {
                sh 'echo test'
                sh 'echo test2'
            }
        }
    }
}
